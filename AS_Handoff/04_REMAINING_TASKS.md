# 남은 작업 및 개선 아이디어 (2026-04-21 기준)

## 긴급 수정 필요 (현재 세션 미해결)

### 1. valk_sk1 이펙트 — Unity 기즈모 아티팩트
- **현상**: 흰색 가로/세로 축선 + 스피커 아이콘이 이펙트 프레임에 그대로 노출됨
- **원인**: Unity Game View에서 "Gizmos" 버튼이 ON 상태로 캡처됨
- **해결 방법**:
  - **(추천)** Unity Editor에서 Game View "Gizmos" 버튼 끄고 재캡처 (`EffectCaptureValkSk1.cs` 실행)
  - **(차선)** Python `make_fx_sprites.py`에 near-white 픽셀 제거 로직 추가

### 2. valk_sk1 이펙트 — Z-인덱스 (플레이어 뒤에 표시)
- **현상**: 이펙트가 발키리(z-index:18) 뒤에 렌더링됨 (이펙트 z-index:15)
- **수정**: `playValkSprite()`의 `z-index:15` → `z-index:20`으로 변경

### 3. valk_sk1 이펙트 — 크기 작음
- **현상**: 460px 표시로는 임팩트 부족
- **수정**: main=600×600, body=420×420, weapon=420×420으로 확대

### 4. valk_sk1 이펙트 — 방향 미처리
- **현상**: 이펙트가 항상 특정 방향(Unity 기본 방향)으로만 나옴
- **수정**: `playValkSprite()` 호출 시 `flipX: player.facing === 'left'` 적용

### 5. 발키리 애니메이션 — 정면 공격 크기 불일치
- **현상**: 정면(front) 공격 시 이미지 크기가 다른 방향(idle/move/left/right 공격)보다 작음
- **수정**: CSS 또는 JS에서 정면 공격 스프라이트 크기를 다른 방향과 통일

### 6. 발키리 애니메이션 — 스킬 애니메이션 오작동
- **현상**: E스킬 발동 시 모션이 제대로 재생 안 되고 마지막 프레임만 표시됨
- **수정**: `valkPlayAnim('skill_e')` 호출 흐름 및 스프라이트 시트 프레임 수 확인

### 7. 발키리 애니메이션 — 뒤돌아보기 노출
- **현상**: 특정 상황에서 뒤를 보는 애니메이션이 보임 (의도하지 않은 상태)
- **수정**: 방향 전환 로직 및 애니메이션 스테이트 머신 점검

## 즉시 가능한 수정

### 8. 이펙트 컬러 검증
- **현상**: 마젠타 크로마키 후 일부 색상이 원본과 다름
- **확인**: `CHROMA_THRESHOLD=130`, `g < 120` 임계값 재조정 또는 더 정밀한 키잉 필요

## 콘텐츠 기획 (추후)

### 시설 업그레이드 시스템
- 현재: spec.html에 "업데이트 예정" 뱃지
- 기획 필요: Lv.1→2→3 업그레이드 스탯 설계
- 구현 필요: index.html에 시설 레벨업 로직 추가

### 난이도 시스템 수치 검증
- 성 레벨 기반 난이도 배율 (×1.0 ~ ×2.5) 인게임 구현

### 결과 화면 상세화
- DPS 그래프, Wave별 통계, 킬 수

### 모바일 터치 조작 지원

## GitHub 관련

### 스크린샷 최신화 필요
- v1.8.0 업데이트 후 스크린샷 아직 미갱신 (위 수정 완료 후 진행)

### ZIP 자동 생성
- GitHub Actions로 push 시 자동 ZIP 생성 가능 (현재는 수동)
